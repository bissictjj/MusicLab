<!-- A DrawerLayout is intended to be used as the top-level content view using match_parent for both width and height to consume the full space available. -->
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:id="@+id/drawer_layout"
    android:layout_width="match_parent" android:layout_height="match_parent"
    tools:context=".HomeActivity">

    <!-- As the main content view, the view below consumes the entire
         space available using match_parent in both dimensions. -->
    <RelativeLayout android:layout_width="match_parent"
        android:layout_height="match_parent"> 
        
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/container"
            android:layout_above="@+id/ll_musicplayer"/>

        <!--<LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@+id/ll_netconsole"
            android:background="@android:color/holo_orange_light"
            android:gravity="center">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="30dp"
                android:id="@+id/tv_home_display"
                android:layout_above="@+id/et_frag_home"
                android:gravity="center_vertical"/>

            <EditText
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:id="@+id/et_frag_home"
                android:layout_alignParentBottom="true"/>
            </LinearLayout>-->

        <LinearLayout
            android:orientation="horizontal"
            android:id="@+id/ll_netconsole"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:background="@android:color/holo_red_light"
            android:layout_above="@+id/ll_musicplayer"
            android:gravity="center">

            <Button
                android:layout_width="100dp"
                android:layout_height="50dp"
                android:id="@+id/btn_register"
                android:text="Register"/>
               <!-- android:layout_gravity="left"
                android:gravity="left"-->


            <Button
                android:layout_width="100dp"
                android:layout_height="50dp"
                android:id="@+id/btn_send"
                android:text="Send"/>
                <!--android:layout_gravity="right"
                android:gravity="right"-->


            </LinearLayout>
        
        <LinearLayout
            android:id="@+id/ll_musicplayer"
            android:orientation="horizontal"
            android:layout_alignParentBottom="true"

            android:layout_width="match_parent"
            android:layout_height="75dp"
            android:background="@android:color/holo_blue_bright"
            android:gravity="center">

            <ImageButton
                android:layout_height="50dp"
                android:layout_width="50dp"
                android:id="@+id/ib_previous"
                android:layout_marginLeft="20dp"
                android:src="@drawable/ic_action_previous"/>

            <ImageButton
                android:layout_height="50dp"
                android:layout_width="50dp"
                android:id="@+id/ib_play"
                android:layout_margin="10dp"
                android:src="@drawable/ic_action_play"
                />

            <ImageButton
                android:layout_height="50dp"
                android:layout_width="50dp"
                android:id="@+id/ib_next"
                android:layout_marginRight="20dp"
                android:src="@drawable/ic_action_next"/>


            </LinearLayout>

    </RelativeLayout>

    <!-- android:layout_gravity="start" tells DrawerLayout to treat
         this as a sliding drawer on the left side for left-to-right
         languages and on the right side for right-to-left languages.
         If you're not building against API 17 or higher, use
         android:layout_gravity="left" instead. -->
    <!-- The drawer is given a fixed width in dp and extends the full height of
         the container. -->
    <fragment android:id="@+id/navigation_drawer"
        android:layout_width="@dimen/navigation_drawer_width" android:layout_height="match_parent"
        android:layout_gravity="start" android:name="com.afrozaar.musiclab.NavigationDrawerFragment"
        tools:layout="@layout/fragment_navigation_drawer" />

</android.support.v4.widget.DrawerLayout>
